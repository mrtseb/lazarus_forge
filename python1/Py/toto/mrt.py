class Mrt(object):
   
   # définition de la méthode spéciale __init__ (constructeur)
   def __init__(self, nom='TACK', prenom='sebastien'):
      """Initialisation du compte avec la valeur soldeInitial."""
      # assignation de l'attribut d'instance solde
      self.nom = nom
      self.prenom= prenom	  
	  
	  
class Info(object):
   
   # définition de la méthode spéciale __init__ (constructeur)
   def __init__(self):
      """Initialisation du compte avec la valeur soldeInitial."""
      # assignation de l'attribut d'instance solde
      self.info = 'test'